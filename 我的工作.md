# 我的工作

## 1. 前期规划

先通读题目与 Markdown 说明，弄清「销额 / 战区 / 即时零售 / 退损率」对应哪些表字段。

技术选型：SQLite；DeepSeek-V3（七牛 API，Function Calling）；Streamlit。大模型负责规划与写 SQL/结论，Python 负责执行与画图。效果变好，主要靠提示词约束 + 代码门禁，不是换更强模型。

| 文件 | 职责 |
|------|------|
| `src/agent.py` | System Prompt（`SYSTEM_TEMPLATE`）+ ReAct 主循环 |
| `src/schema_pack.py` | 指标口径与 SQL 模板（`METRIC_RULES`） |
| `src/tools.py` | `run_sql` / `plot_chart` / `finish` 等工具实现 |
| `src/db.py` | SQL 执行前门禁 |
| `src/app.py` | 界面：结论、图表、滚动、侧边栏 |
| `src/chat_store.py` | 多会话历史与标题 |

---

## 2. 迭代问题与改动

### 问题 1：示例 3 只画超阈值门店，没有画「各门店」图

**现象**：模型先筛退损率>5%，再画图 → 图上只有两家店。题目要的是全部门店对比，阈值只用于文字和下钻。

**改动要点**：

1. **`agent.py` 提示词**：增加「图表与筛选分离」——全量对比图必须含全部相关门店；「找出超过阈值」只写在文字里。并写死示例 3 流程：双 CTE 查全量 → `plot_chart(threshold_y=5)` → 再分析原因。
2. **`schema_pack.py`**：画图约定写明，阈值筛选不能当对比图唯一数据源。
3. **`tools.py` → `_plot_chart` 的 `threshold_y`**：不把未超阈值的店从数据里丢掉，只给超阈值柱换色，并画红色阈值虚线。
4. **同函数门店覆盖检查**：标题含「各门店/每家门店」但图中门店数少于库里门店数 → 返回 warning，催模型用全量结果重画。
5. **`app.py` 示例 3 措辞**：改成先写「画出全部门店退损率对比图」，再写「找出超过 5%」。

**结果**：四家店都在图上，超 5% 的被高亮。

---

### 问题 2：示例 5 放量漏判、数字和结论打架

**现象**：乱抽几个商品，或 `INNER JOIN` 丢掉只在一季有销售的 SKU → 科技园放量常漏掉；即便算出「增长 3 倍」，仍可能写「未见显著放量」。

**改动要点**：

1. **`agent.py` 提示词**：固定最短路径（筛店→门店图→全量 SKU SQL→SKU 图→finish）；下钻必须用 Q1/Q2 **并集（UNION）+ LEFT JOIN**，禁止 LIMIT/抽样；放量标准写死——同一 SKU `(Q2-Q1)/Q1 ≥ 1` 必须称显著放量；禁止「某店通常不放量」的先验，两店同一套规则。
2. **`schema_pack.py`**：放入可仿写的低毛利 SKU 全量 SQL 模板。
3. **`tools.py` → `finish` 工具说明**：强调按本次数字逐店判定；增长翻倍以上必须写显著放量；**不要默认科技园店不存在放量**。
4. **`tools.py` → `_sku_coverage_warning`**（画图成功后调用） 
   低毛利下钻时，两家店都该把「够条件的商品」列全。如果星河路店画了好几个 SKU，科技园店却只剩一两个，多半是 SQL 抽样本不全，容易漏判放量。 
   **代码逻辑**：统计每店有几个 `product_name`；若一家很少、另一家很多（例如 min≤2 且 max≥4），或总行数明显偏少 → 在 `plot_chart` 返回里加 `warning`，下一轮模型可读到并重查。两店都是均衡的 2+2（本数据正常全量）则不报警。

**结果**：两店显示器/激光打印机都会进结果，科技园也会判为显著放量。

---

### 问题 3：示例 3 退损率算成 70%+（分母写错）

**错误写法（退款表驱动、同时汇总销额）**：从 `fact_refund` 出发 JOIN `fact_sales`，同一层既 `SUM(退款)` 又 `SUM(销额)`。这样分母变成「退过款的订单销额」，比率虚高。

**正确写法**：两个 CTE（`WITH ... AS (...)` 临时表）——`sales` 按全部成交汇总，`refunds` 按退款汇总，再相除。

**改动要点**：

1. **`schema_pack.py`**：写清口径 + 正确双 CTE 模板 + 本数据通常个位数、>30% 必错。
2. **`db.py` → `_find_refund_rate_denominator_trap`**：执行前扫描 SQL，发现「FROM fact_refund + JOIN fact_sales，且同时算退款和销额」→ 直接拒收。
3. **`tools.py` → `_implausible_refund_rate_error`**：SQL 若跑过了，但结果退损率仍高得离谱（如 >30%）→ `_run_sql` 返回失败，逼模型重写。
4. **`agent.py`**：易错点里写明正确量级（科技园≈8%、星河路≈5.7%）。

**结果**：退损率回到合理量级。

---

### 问题 4：多图时图1/图2 错位

**现象**：模型自己在 title 写「图1」，结果图片内容为图2；总结中的图片顺序出错，先出现图2，后出现图1。

**改动要点**（提示词软禁止 + 代码硬纠正）：

1. **`agent.py`**：title 不要自写「图N」；禁止「图表展示」清单；正文只用「见图1」「见图2」。
2. **`tools.py` → `_plot_chart`**：用正则剥掉模型自带的「图N：」；按**成功画图顺序**重编号（`fig_idx = len(figures)+1`），写入图内标题。
3. **`app.py` → `_render_answer_with_figures`**：固定按生成顺序渲染图1→图2，不再按正文穿插。
4. **`app.py` → `_rewrite_figure_refs`**：用正则删掉正文里「图表展示：……」整段（删到「业务建议」或下一标题为止）；并把「下图为××」改成「如图N所示」。

一句话：提示词管「别乱写」；工具管「图号以谁先画成为准」；界面管「写了清单也删掉、按序展示」。

---

### 问题 5：SKU 图横轴只有门店名

**现象**：示例 5 低毛利下钻结果里明明有 `store_name` 和 `product_name`，但 `plot_chart` 仍用 `x=store_name`。同一家店多个 SKU（如激光打印机、24 寸显示器）被叠在同一根柱上，图上只看见店名，看不出是哪个商品；Q1/Q2 对比也会被错误聚合。

**改动要点**：

1. **`tools.py` → 新增 `_ensure_store_sku_axis(df, x, y)`**，在 `_plot_chart` 选轴时调用。 
   只要这张表是「门店 + 商品」粒度，就不能只拿店名当横轴。 
   **逻辑**：若同时存在 `store_name`、`product_name`，且模型传的是 `x=store_name`（或 y 为多列、或 x 为空/商品列），则新建列 
   `门店_SKU = store_name + " - " + product_name`，并**强制**把横轴改成 `门店_SKU`。模型写错参数也会被改掉。
2. **`agent.py` 易错点**：写明 SKU 对比 SQL 必须带这两列；禁止只用 `product_name` 把两家店混在一根柱里。

**结果**：图 2 横轴形如「星河路店 - 激光打印机」「科技园店 - 24寸显示器」，店和商品都能看清。

---

### 问题 6：没有多会话历史

**现象**：侧边栏最初只有「清空对话」。刷新页面就会失去之前对话的过程、结论和图表，无法对照，也不能回来接着看。

**改动要点**：

1. **新增 `src/chat_store.py`**，会话持久化到 `data/chat_history.json`。 
   主要函数：`load_store` / `save_store` 读写文件；`start_new_conversation` 新建；`delete_conversation` / `rename_conversation` 删除与重命名；`sync_current_messages` 把当前聊天内容写回当前会话。 
   图表用 `_serialize_figures` / `_deserialize_figures`（Plotly `to_json` / `from_json`）一起落盘，刷新页面后图还在。
2. **`app.py` 侧边栏**：接上新建、切换、重命名、删除；启动时 `load_store()` 恢复历史。
3. **短标题：`summarize_title(首条用户问题)`**（**不用大模型**）  
   - 按关键词规则匹配，例如含「退损」→「门店退损情况分析」，含「季度」+「对比」→「季度销售额对比」；  
   - 对不上规则 → 截取问题标点前的前半句，超长加「…」；  
   - 在 `sync_current_messages()` 里调用；若用户手动改过标题（`title_locked=True`），则不再自动覆盖。

**结果**：示例 1～5 可以并存，侧边栏切换即可回看完整对话和图表。

---

### 问题 7：滚动不跟最新 / 抢滚动

**现象**：Agent 一边跑工具一边往下追加内容时，页面常停在上面，要自己翻到底才看得到最新步骤。后来加上自动滚到底，又出现反过来的问题——用户想回头看前面的 SQL 时，页面被反复拽回底部（旧实现有长达约 4 秒的 `setTimeout` 重试）。

**改动**：`app.py` → `_scroll_to_latest(anchor_id, mode=...)`，通过 `st.html` 注入一小段 JS，按三种模式控制：

| mode | 何时调用 | 行为 |
|------|----------|------|
| `start` | 用户刚发新问题 | 重新允许自动吸底，短延迟跟几次，保证看到本轮最新内容 |
| `follow` | 分析过程中每推进一步 | 仅在用户**没有上翻**时跟随；已上翻则不动 |
| `once` | 分析结束 | 只轻推一次，不再长链重试 |

**怎样算「用户上翻」**：滚轮向上、触摸上拖、或滚动容器 `scrollTop` 变小 → 调用 `pauseAutoScroll()`，设 `__userPausedAutoScroll=true`，并清掉还未执行的跟随定时器。

**结果**：新提问会跟到最新结论；用户主动往上读过程时，滚动条不再被抢走。

---


### 问题 8：示例 4 只画增长超 10% 的两家店

**现象**：与示例 3 同类——把「找出」当成「只画筛出的店」。旧提示词里「可只画筛出店」的例外被模型套到了示例 4，图上只有星河路、科技园两家。

**改动要点**：

1. **`agent.py`**：单独写清示例 4 流程：先全量 Q1/Q2 图，增长>10% 只写在文字里。
2. **`schema_pack.py`**：增加全部门店 Q1/Q2 SQL 模板，注明不要先按 `growth>0.1` 过滤再画图。
3. **`tools.py`（两处，别混在一起）**

   **（a）改 `plot_chart` 工具说明（给模型看的）** 
   在 `TOOL_DEFINITIONS` 里写清楚两类题怎么画：
   
   - 用户问「比较各门店/每家门店，再找出增长>10%」→ **必须用全部门店结果画图**，不能只画过线的店；
   - 用户问「已经筛出的门店再下钻」（如示例 5：增长且毛利率下降 + SKU）→ 可以只画筛出的店。 
   作用：减少模型一上来就 `WHERE growth>0.1` 再 `plot_chart`。
   
   **（b）改 `_plot_chart` 画完后的门店覆盖检查（代码硬检查）** 
   检查规则本身一直是： 
   `标题含「各门店/每家门店/全部门店」` 且 `图中门店数 < dim_store 门店总数` → 加 `warning`。  
   
   **旧逻辑多了一道短路（有 bug）**：若标题里还出现「增长 / 超过 / 下降 …」，就认为「这是筛选图」，**整段检查跳过**。 
   但示例 4 正确标题是： 
   `2025年各门店Q1与Q2销售额对比（星河路店和科技园店增长超10%）` 
   ——既有「各门店」，又有「增长」。结果：图只画了 2 家店，检查却被「增长」二字关掉 → **漏报**。  
   
   **新逻辑**：删掉这道「标题含增长就跳过」的短路。只要标题声称是各门店对比、门店数又不够，就 warning，逼模型用未筛选的全量结果重画。括号里的「……增长超10%」只当结论，不当成少画店的理由。

**结果**：四家店 Q1/Q2 并排柱，文字再点名星河路、科技园。

---

### 问题 9：写「增长」箭头却大→小

**现象**：`增长 89.64%（78.69万 → 41.49万）`，百分比与箭头方向矛盾。

**改动**：主要在 `agent.py` 提示词——写「增长/下降（A→B）」时必须 A=Q1、B=Q2，并给正反例。目前没有单独的事后正则改写函数。

---

## 3. 小结

| 手段 | 代表位置 | 作用 |
|------|----------|------|
| 提示词 | `agent.py` / `schema_pack.py` | 规定正确流程与禁止事项 |
| SQL/结果门禁 | `db.py` / `_implausible_refund_rate_error` 等 | 错 SQL、假高退损率跑不出去 |
| 画图防呆 | `_plot_chart` / `_ensure_store_sku_axis` 等 | 图号、横轴、全量覆盖 |
| 界面纠正 | `_rewrite_figure_refs` / `_scroll_to_latest` / `chat_store` | 文图不错位、好用 |

