# 我的工作

## 1. 前期规划

先通读题目与 Markdown 说明，弄清「销额 / 战区 / 即时零售 / 退损率」对应哪些表字段。

技术选型：SQLite；DeepSeek-V3（七牛 API，Function Calling）；Streamlit。大模型负责规划与写 SQL/结论，Python 负责执行与画图。效果变好，主要靠提示词约束 + 代码门禁，不是换更强模型。

| 文件 | 职责 |
|------|------|
| `src/agent.py` | System Prompt（`SYSTEM_TEMPLATE`）+ ReAct 主循环 |
| `src/schema_pack.py` | 指标口径与 SQL 模板（`METRIC_RULES`） |
| `src/tools.py` | `run_sql` / `plot_chart` / `finish` 等工具实现 |
| `src/db.py` | SQL 执行前门禁 |
| `src/app.py` | 界面：结论、图表、滚动、侧边栏 |
| `src/chat_store.py` | 多会话历史与标题 |

---

## 2. 迭代问题与改动

### 问题 1：示例 3 只画超阈值门店，没有画「各门店」图

**现象**：模型先筛退损率>5%，再画图 → 图上只有两家店。题目要的是全部门店对比，阈值只用于文字和下钻。

**原因**：题目里同时有「画出各门店对比图」和「找出超过 5%」。模型把两句话合成一步——先 `WHERE 退损率>5%`，再用这个子集画图。当时提示词没有把「画全量图」和「阈值筛选」拆开，也没有强制「图用全量、筛选只写文字」的流程，工具层也不会拦「标题写各门店、图却只有两家店」。

**改动要点**：

1. **`agent.py` 提示词**：增加「图表与筛选分离」——全量对比图必须含全部相关门店；「找出超过阈值」只写在文字里。并写死示例 3 流程：双 CTE 查全量 → `plot_chart(threshold_y=5)` → 再分析原因。
2. **`schema_pack.py`**：画图约定写明，阈值筛选不能当对比图唯一数据源。
3. **`tools.py` → `_plot_chart` 的 `threshold_y`**：不把未超阈值的店从数据里丢掉，只给超阈值柱换色，并画红色阈值虚线。
4. **同函数门店覆盖检查**：标题含「各门店/每家门店」但图中门店数少于库里门店数 → 返回 warning，催模型用全量结果重画。
5. **`app.py` 示例 3 措辞**：改成先写「画出全部门店退损率对比图」，再写「找出超过 5%」。

**结果**：四家店都在图上，超 5% 的被高亮。

---

### 问题 2：示例 5 放量漏判、数字和结论打架

**现象**：乱抽几个商品，或 `INNER JOIN` 丢掉只在一季有销售的 SKU → 科技园放量常漏掉；即便算出「增长 3 倍」，仍可能写「未见显著放量」。

**原因**（两层）：

1. **数据不全**：模型习惯只挑几个商品，或用 `INNER JOIN` 只保留两季都有的 SKU，单季才有量的商品被丢掉 → 科技园的低毛利放量查不全。
2. **判定不统一**：提示词里没有写死「`(Q2-Q1)/Q1 ≥ 1` 就必须称显著放量」，也没有禁止「某店通常不放量」这类先验。早期漏判后，模型会带着「科技园不放量」的印象写结论，于是数字已是翻倍，文字仍写「未见显著放量」。

**改动要点**：

1. **`agent.py` 提示词**：固定最短路径（筛店→门店图→全量 SKU SQL→SKU 图→finish）；下钻必须用 Q1/Q2 **并集（UNION）+ LEFT JOIN**，禁止 LIMIT/抽样；放量标准写死——同一 SKU `(Q2-Q1)/Q1 ≥ 1` 必须称显著放量；禁止「某店通常不放量」的先验，两店同一套规则。
2. **`schema_pack.py`**：放入可仿写的低毛利 SKU 全量 SQL 模板。
3. **`tools.py` → `finish` 工具说明**：强调按本次数字逐店判定；增长翻倍以上必须写显著放量；不要默认科技园店不存在放量。
4. **`tools.py` → `_sku_coverage_warning`**（画图成功后调用） 
   低毛利下钻时，两家店都该把「够条件的商品」列全。如果星河路店画了好几个 SKU，科技园店却只剩一两个，多半是 SQL 抽样不全，容易漏判放量。 
   **代码逻辑**：统计每店有几个 `product_name`；若一家很少、另一家很多（例如 min≤2 且 max≥4），或总行数明显偏少 → 在 `plot_chart` 返回里加 `warning`，下一轮模型可读到并重查。两店都是均衡的 2+2（本数据正常全量）则不报警。

**结果**：两店显示器/激光打印机都会进结果，科技园也会判为显著放量。

---

### 问题 3：示例 3 退损率算成 70%+（分母写错）

**现象**：退损率算到 70% 以上，和零售常识不符。

**原因**：模型常用「从退款表出发」的 SQL：`FROM fact_refund JOIN fact_sales`，同一层既 `SUM(退款)` 又 `SUM(销额)`。JOIN 进来的销额只覆盖**发生过退款的订单**，分母被缩小，比率虚高。正确口径应是：分母 = 该门店该时段**全部成交销额**（销额、退款分两个 CTE 汇总后再除）。当时 Schema Pack 虽有指标定义，但缺少可仿写的正确模板，执行层也不拦截这种错误写法。

**错误写法**：退款表驱动、同时汇总销额（见上）。 
**正确写法**：两个 CTE——`sales` 按全部成交汇总，`refunds` 按退款汇总，再相除。

**改动要点**：

1. **`schema_pack.py`**：写清口径 + 正确双 CTE 模板 + 本数据通常个位数、>30% 必错。
2. **`db.py` → `_find_refund_rate_denominator_trap`**：执行前扫描 SQL，发现「FROM fact_refund + JOIN fact_sales，且同时算退款和销额」→ 直接拒收。
3. **`tools.py` → `_implausible_refund_rate_error`**：SQL 若跑过了，但结果退损率仍高得离谱（如 >30%）→ `_run_sql` 返回失败，逼模型重写。
4. **`agent.py`**：易错点里写明正确量级（科技园≈8%、星河路≈5.7%）。

**结果**：退损率回到合理量级。

---

### 问题 4：多图时图1/图2 错位

**现象**：模型自己在 title 写「图1」，结果图片内容是图2；总结里先出现图2、后出现图1。

**原因**（三处叠在一起）：

1. 模型在 `plot_chart` 的 title 里自己写「图1」「图2」，编号和真实画图顺序不一致；
2. 正文再写一段「图表展示：图1……图2……」清单，容易和实际图面错位；
3. 界面曾按正文**先提到哪个图号**来插图——文字先写「见图2」，图2就被插到图1上面。

根因是：图号既靠模型临场发挥，展示顺序又跟着正文走，没有「以成功画图顺序为准」的硬规则。

**改动要点**（提示词软禁止 + 代码硬纠正）：

1. **`agent.py`**：title 不要自写「图N」；禁止「图表展示」清单；正文只用「见图1」「见图2」。
2. **`tools.py` → `_plot_chart`**：用正则剥掉模型自带的「图N：」；按**成功画图顺序**重编号（`fig_idx = len(figures)+1`），写入图内标题。
3. **`app.py` → `_render_answer_with_figures`**：固定按生成顺序渲染图1→图2，不再按正文穿插。
4. **`app.py` → `_rewrite_figure_refs`**：用正则删掉正文里「图表展示：……」整段；并把「下图为××」改成「如图N所示」。

一句话：提示词管「别乱写」；工具管「图号以谁先画成为准」；界面管「写了清单也删掉、按序展示」。

---

### 问题 5：SKU 图横轴只有门店名

**现象**：示例 5 低毛利下钻结果里明明有 `store_name` 和 `product_name`，但图上只有店名，看不出是哪个 SKU；同店多个商品叠在一根柱上。

**原因**：模型画 SKU 图时习惯传 `x=store_name`（门店级图的惯性），工具层又原样照做。结果集虽是「门店×商品」多行，横轴却按店聚合 → 同一家店的多个 SKU 叠成一根柱，商品名被吃掉。当时没有「有 product_name 就强制改成门店-商品轴」的防呆。

**改动要点**：

1. **`tools.py` → 新增 `_ensure_store_sku_axis`**，在 `_plot_chart` 选轴时调用。 
   只要同时有 `store_name`、`product_name`，且模型传了 `x=store_name`（或 y 多列等），就强制 
   `门店_SKU = 店名 + " - " + 商品名` 作为横轴，参数写错也会被改掉。
2. **`agent.py` 易错点**：SKU 对比 SQL 必须带这两列；禁止只用 `product_name` 把两家店混柱。

**结果**：横轴形如「星河路店 - 激光打印机」，店和商品都能看清。

---

### 问题 6：没有多会话历史

**现象**：侧边栏最初只有「清空对话」。刷新页面或换题后，之前的过程、结论和图表都没了。

**原因**：早期只把对话放在 Streamlit 的 `st.session_state` 里，没有落盘。刷新/重启会清空内存；也没有「多会话」数据结构。属于功能缺失，不是模型推理错误。

**改动要点**：

1. **新增 `chat_store.py`**：会话落到 `data/chat_history.json`；`load_store`/`save_store` 读写；新建/删除/重命名；`sync_current_messages` 同步当前聊天；图表用 Plotly JSON 序列化，刷新后还能还原。
2. **`app.py` 侧边栏**：接上新建、切换、重命名、删除；启动时加载历史。
3. **`summarize_title()`**：不用大模型，按首条问题关键词生成短标题（如「退损」→「门店退损情况分析」）；用户手动改过的标题（`title_locked`）不再覆盖。

**结果**：示例 1～5 可并存，切换即可回看。

---

### 问题 7：滚动不跟最新 / 抢滚动

**现象**：分析过程中页面常停在上面，要自己翻到底；加上自动吸底后，用户上翻读内容时又被反复拽回底部。

**原因**：

1. **不跟随**：Streamlit 流式追加内容时，视口不会自动滚到最新一步，没有「跟当前输出」的逻辑。
2. **抢滚动**：后来用 JS 自动滚到底，但旧实现有长达约 4 秒的 `setTimeout` 重试，且**不检测用户是否在上翻**。用户想看上面的 SQL 时，定时器仍在把视口拽回去。

**改动**：`app.py` → `_scroll_to_latest(mode=...)`：

| mode | 何时 | 行为 |
|------|------|------|
| `start` | 新提问 | 重新允许吸底，短延迟跟几次 |
| `follow` | 过程中每步 | 仅当用户未上翻时跟随 |
| `once` | 结束 | 轻推一次，无长重试 |

上翻（滚轮向上 / 触摸上拖 / `scrollTop` 变小）→ 暂停自动跟随并清掉未执行定时器。

**结果**：新提问跟到最新；上翻阅读时不再被抢滚动。

---

### 问题 8：示例 4 只画增长超 10% 的两家店

**现象**：图上只有星河路、科技园两家，没有天府店、湖滨店。

**原因**：和问题 1 同类——模型把「比较每家门店」和「找出增长>10%」合成「先筛再画」。当时提示词还有一句例外：「找出满足条件的门店再分析，图可以只画筛出的店」（本意给示例 5），模型把示例 4 也套进去了。另外，门店覆盖检查有 bug：标题里一旦出现「增长」就跳过检查；而正确标题常写「各门店……（……增长超10%）」→ 只画两家店也不会 warning。

**改动要点**：

1. **`agent.py`**：写清示例 4 流程——先全量 Q1/Q2 图，增长>10% 只写在文字里；例外收窄为仅示例 5 可只画筛出店。
2. **`schema_pack.py`**：全部门店 Q1/Q2 SQL 模板，注明不要先按 `growth>0.1` 再画图。
3. **`tools.py`**：
   - **（a）工具说明**：比较各门店再找阈值 → 必须画全量；示例 5 下钻才可只画筛出店。
   - **（b）覆盖检查**：删掉「标题含增长就跳过」的短路。只要标题有「各门店」且门店数不够，就 warning。

**结果**：四家店 Q1/Q2 并排柱，文字再点名增长超 10% 的店。

---

### 问题 9：写「增长」箭头却大→小

**现象**：`增长 89.64%（78.69万 → 41.49万）`——百分比是涨，箭头却是大→小，读起来像下降。

**原因**：模型写结论文案时，常把两个金额按「先看到的行 / 表格顺序」排列，而不是按时间顺序。SQL 或中间表若 Q2 行在前，就容易写成「Q2 → Q1」。提示词里原先没有「增长/下降（A→B）必须 A=Q1、B=Q2」的硬规则，也没有事后改写，所以方向错误会直接进正文。

**改动**：主要在 `agent.py` 提示词——写「增长/下降（A→B）」时必须 A=Q1、B=Q2，并给正反例。目前没有单独的事后正则改写函数。

**结果**：增长句固定为「Q1 小 → Q2 大」。

---

### 问题 10：分渠道对比图变成「每店一根堆叠柱」，而不是「每店多根并排柱」

**现象**：题目如「查询 2025 年上半年每家门店在到店、即时零售、团购三个渠道的销售额，生成分渠道对比柱状图」。改前每个门店只有**一根**柱，B2B / O2O / POS 上下堆叠（堆叠柱）；改后每个门店有**三根并排柱**，三个渠道并排可比（分组柱）。

**原因**：`plot_chart` 用 `x=store_name`、`color=channel_code` 画柱图时，是否堆叠由 `_should_stack_bars()` 决定，再设 Plotly 的 `barmode` 为 `"stack"` 或 `"group"`。旧逻辑里，只要每个 x（门店）下 `color` 取值个数 ≥2，就倾向堆叠——这适合「店内结构/占比」类图，但不适合「分渠道对比」。渠道对比需要各渠道从 0 起画、并排比高低；一堆叠，上面两段没有共同基线，跨店对比 POS/O2O 就很难看。当时没有把 `channel_code`（以及 Q1/Q2 的 `系列`）从「可堆叠」里排除出去。

**改动要点**：

1. **`tools.py` → `_should_stack_bars(df, x, color, title)`**  
   - `color` 为 `channel_code` / `channel` / `渠道`（或列名含 channel/渠道）→ **强制不堆叠**，走分组柱；  
   - `color` 为 `系列` / `quarter`（Q1/Q2 熔后的系列）→ 同样强制不堆叠；  
   - 仅在商品/品类结构，或标题含「结构 / 占比 / 构成」等组成类语义时，才允许堆叠。
2. **`_plot_chart`**：`barmode = "stack" if stack_bars else "group"`；若 `color == "系列"`，再保险地设 `stack_bars = False`。

**结果**：分渠道题每个门店三根并排柱（B2B / O2O / POS），便于跨店、跨渠道直接比销售额；店内 SKU 结构类图仍可堆叠。

---

## 3. 小结

多数问题不是「模型完全不会」，而是：

- **提示词没把易混步骤拆开**（全量图 vs 阈值筛选、图号归属、放量判定）；
- **SQL/画图没有门禁**（退损率分母、SKU 横轴、门店覆盖）；
- **界面缺工程能力**（会话落盘、滚动策略）。

| 手段 | 代表位置 | 作用 |
|------|----------|------|
| 提示词 | `agent.py` / `schema_pack.py` | 规定正确流程与禁止事项 |
| SQL/结果门禁 | `db.py` / `_implausible_refund_rate_error` 等 | 错 SQL、假高退损率跑不出去 |
| 画图防呆 | `_plot_chart` / `_ensure_store_sku_axis` / `_should_stack_bars` 等 | 图号、横轴、全量覆盖、渠道用分组柱不堆叠 |
| 界面纠正 | `_rewrite_figure_refs` / `_scroll_to_latest` / `chat_store` | 文图不错位、好用 |
