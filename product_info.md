# product_info.xlsx 数据说明

`product_id`：商品唯一编号；`product_name`：商品名称；`category`：商品品类；`unit_cost`：单位成本（元）；`list_price`：标准标价（元）。

业务中常将一个商品编号称为一个 `SKU`。因此“SKU”一般需要对应 `product_id`。

`3C配件` 指电脑、通信及消费电子相关配件，本题直接作为一个商品品类使用。

